{
  "name": "retrn-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Retrn browser extension — log emails/interactions to a contact's Retrn profile from Gmail & Outlook.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "zip": "node build.mjs && cd dist && zip -r ../retrn-extension.zip . && cd .."
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "@types/chrome": "^0.0.270"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
