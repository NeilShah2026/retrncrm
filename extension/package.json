{
  "name": "retrn-extension",
  "version": "0.3.0",
  "private": true,
  "description": "Retrn browser extension — log Gmail and Outlook emails and LinkedIn contacts to your Retrn network.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "zip": "node build.mjs && cd dist && zip -r ../retrn-extension.zip . && cd ..",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.1",
    "preact": "^10.29.8"
  }
}
